#include <stdio.h>
int main()
{
    int a, b;
    scanf("%d,%d", &a, &b);
    printf("%d", a * 10 + b);
    return 0;
}
/* char A[5];
    scanf("%s", A);
    printf("%d", (A[0] - '0') * 10 + (A[2] - '0')); */